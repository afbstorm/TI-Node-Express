doctype html
html
    head
        title Products list
        link(rel='stylesheet', href='/styles.css')
    body
        include header.pug
        section#content
            each product in products
                h2: a(href=`/details/${product.id}`)= product.nom
                p= product.description
                p= product.prix
        include footer.pug
